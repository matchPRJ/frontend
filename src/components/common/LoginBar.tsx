import '../../css/Common.css'

const LoginBar = () => {
    return(
        <div className="login">
            <span>로그인</span>
            <span> / </span>
            <span>회원가입</span>
        </div>
    );
}

export default LoginBar;